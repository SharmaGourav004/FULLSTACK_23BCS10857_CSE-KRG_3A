spring.application.name=backend

# --- Datasource (development) ---
# Using H2 in-memory database for local development. To use MySQL, replace the following properties with your MySQL URL/user/password.
spring.datasource.url=jdbc:mysql://localhost:3306/petcare
spring.datasource.username=root
spring.datasource.password=Gourav@2004
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Database schema management: 'update' will modify existing tables without dropping data
# Options: create (drop and recreate), create-drop, update (recommended), validate, none
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true


# --- JWT settings ---
# Replace this secret with a long random value in production. You can also set via environment variable APP_JWT_SECRET
app.jwt.secret=change-this-dev-secret-please-1234567890
app.jwt.expiration-ms=3600000

# --- Cloudinary (placeholders) ---
# Set these values in production or in application.properties for uploads
cloudinary.cloud-name=delcltfew
cloudinary.api-key=828928575397895
cloudinary.api-secret=evfcoq--D1qPS960yrtYOMIPKlo

# --- File Upload Configuration ---
# Maximum file size for uploads (default is 1MB)
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.file-size-threshold=2KB

# Expose H2 console for development
spring.h2.console.enabled=false
spring.h2.console.path=/h2-console

# CORS (Vite default origins)
app.cors.allowed-origins=http://localhost:5173,http://127.0.0.1:5173
